spring:
  application:
    name: soul-frame-gateway
  profiles:
    active: dev
#  main:
#    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: http://111.228.52.114:8848
#        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
      config:
        server-addr: http://111.228.52.114:8848
#        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yaml
        username: nacos
        password: nacos
  config:
    import:
      - optional:nacos:${spring.application.name}.yml
      - optional:nacos:application-${spring.profiles.active}.yml
logging:
  level:
    com.alibaba.nacos: DEBUG

# 接口白名单
router:
  whitelist:
    - /auth/login
    - /auth/login2
    - /auth/captcha
    - /doc.html
    - /webjars/**
    - /swagger-ui/**
    - /v3/api-docs/**
    - /swagger-resources/**
    - /system/config/configGroup
    - /quartz/**
    - /ws/**
    - /error
    - /blog/**
