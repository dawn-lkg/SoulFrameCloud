
spring:
  application:
      name: soul-frame-modules-system
  profiles:
    active: dev
  config:
    import:
      - optional:nacos:application-${spring.profiles.active}.yml
  cloud:
    nacos:
      discovery:
        server-addr: 111.228.52.114:8848
        username: nacos
        password: nacos
      config:
        server-addr: 111.228.52.114:8848
        group: DEFAULT_GROUP
        username: nacos
        password: nacos
  main:
    allow-bean-definition-overriding: true
server:
  port: 9990
