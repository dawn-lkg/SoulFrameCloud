spring:
  application:
    name: soul-frame-modules-notice
  profiles:
    active: dev
  config:
    import:
      - optional:nacos:application-${spring.profiles.active}.yml
  cloud:
    nacos:
      discovery:
        server-addr: 111.228.52.114:8848
        username: nacos
        password: nacos

server:
  port: 9993